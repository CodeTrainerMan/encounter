# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 本地开发服务的输出日志（如 .dev.out.log / .dev.err.log）
*.log

# env files
# 只放行 .env.example（纯占位符）；其余 .env* 一律不入库——
# .env / .env.local / .env.production / .env.development 都可能写进真实密钥
.env
.env*
!.env.example

# vercel
.vercel

# 本地数据导出与种子数据（pg_dump / 备份等），不要入库
/data

# typescript
*.tsbuildinfo
